/**
 * Array.prototype.reverse
 * @description 由于 Array 对象内置了 reverse 函数，先采用这个方法看看效率。然后自行实现 reverse
 * @date
 * @status Accepted
 * @runtime
 * @memory
 */
export function reverseString(s: string[]): void {
  s.reverse();
}
